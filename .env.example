# 数据获取 API
TUSHARE_TOKEN=your_tushare_token_here

# Deepseek API（Agent 系统 LLM）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 代理设置（可选，用于访问国外 API）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# HuggingFace 缓存路径（可选，默认使用系统缓存）
# 如果 C 盘空间不足，可以设置到其他盘
# HF_HOME=F:\huggingface_cache

# 数据库路径（可选，默认使用项目根目录）
# DB_PATH=finance_analysis.db

# 模型路径（可选，默认使用 models/ 目录）
# BERT_MODEL_PATH=models/bert_3cls/best
# EMBEDDING_MODEL_NAME=BAAI/bge-m3

# Chroma 向量库路径（可选，默认使用 data/reports/chroma_db）
# CHROMA_DB_PATH=data/reports/chroma_db

# 日志级别（可选，默认 INFO）
# LOG_LEVEL=INFO
